# Memory Index
## project — gitdash-migration
GitDash 项目采用 Tauri 2 + Vue 3 + TypeScript + Vite 技术栈。移植工作流要求保留新项目的 `package.json`, `vite.config.ts`, `tsconfig.json` 等基础配置，仅将老项目的源码（前端组件、Pinia 状态、Rust...
## project — active-initiative
当前工作流：error[E0599]: no method named `emit` found for reference `&R` in the current scope
## feedback — output-feedback
Use `import.meta.dirname` instead